module.exports = function(args){
	let ret = `systemLog:
  destination: file
  path: /things/mongo.log
  logAppend: true

${(args.security || args.s || args.sec) ? `security:
  authorization: enabled
` : ''}
storage:
  dbPath: /things/data
`

	return ret
}